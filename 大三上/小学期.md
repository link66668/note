## 加壳如何判断
通过查找文件中出现 `UPX0`、`UPX1`，就支持“UPX 壳”的判断；出现 `.mpress`，就支持“MPRESS 壳”的判断。
如果找不到就用die查找壳类型
## 加壳支持
- upx
- confuserex
- mpress闭源
- fsg闭源

## 混淆支持
**3. 混淆类型：既找工具留下的标记，也看内容被怎样弄乱**

代码里有几种具体办法：

- **找混淆工具的标记。**例如部分 .NET 程序经过 ConfuserEx 处理后，会留下 `ConfusedByAttribute`。系统会搜索这个标记，作为识别依据。
- **检查文字是否异常。**对部分 .NET 程序，检查存放文字的区域。如果很多内容不像正常可读文字，会增加“可能存在混淆”的怀疑，但这条线索本身比较弱。
- **检查执行顺序是否被绕乱。**系统会查看程序各段代码之间怎么跳转。如果很多段代码都反复回到同一个“中转站”，再由它分配下一步，就认为可能存在故意打乱执行顺序的混淆。它还会排除一些正常的汇合情况，减少误判。

最后这种检查比较耗时，所以只对符合条件的文件做；已经明显识别出壳时，通常先处理壳。



**3. 根据检查结果，选择对应工具。**

例如：

- 发现是 UPX 包装，就先尝试 UPX 的拆包工具。
- 拆不开、文件又符合条件，就尝试用 unipacker，在模拟环境里让程序完成解包过程，再保存展开后的内容。
- 对部分 .NET 程序，使用 de4dot 尝试整理被弄乱的内容。
- 对隐藏的文字，使用 FLOSS 尝试恢复。
- 对被故意绕乱的执行顺序，也有对应工具尝试整理，不过这部分目前仍存在只能完成一部分的情况。




按当前代码和项目已有验证记录，**主要支持部分 Windows 程序的脱壳、.NET 反混淆、隐藏文字恢复，以及有限的执行顺序整理。**“接入了工具”并不代表所有样本都能处理成功。

**脱壳部分：**

| 类型                     | 当前能力                                 | 已有验证情况                 |
| ---------------------- | ------------------------------------ | ---------------------- |
| **UPX**                | 支持部分 32 位、64 位程序；部分 32 位文件拆不开时会换工具尝试 | 两个已记录样例脱壳成功            |
| **FSG**                | 支持尝试处理 32 位程序                        | 有脱壳成功样例                |
| **MPRESS**             | 支持尝试处理 32 位程序                        | 历史记录有成功脱壳阶段，但存在重复识别等问题 |
| **ASPack**             | 支持尝试处理 32 位程序                        | 已记录复测样例只恢复了文字，不能算脱壳成功  |
| **Enigma Virtual Box** | 支持尝试还原被打包的程序、提取其中的文件，覆盖 32 位、64 位    | 已在人工构造的测试文件上验证         |
|                        |                                      |                        |

这里的 **Enigma Virtual Box 与 Enigma Protector 是两种不同的保护**，后者目前没有自动去除能力。

**反混淆部分：**

| 类型                        | 当前能做什么                   | 限制                       |
| ------------------------- | ------------------------ | ------------------------ |
| **Confuser / ConfuserEx** | 尝试整理被混淆的 .NET 程序         | 有样例产物通过结构检查，但不证明所有混淆都消失  |
| **其他部分 .NET 混淆**          | 调用 de4dot 尝试处理           | 具体效果取决于保护方式，不能宣称全面支持     |
| **隐藏、加密或临时拼出来的文字**        | 使用 FLOSS 恢复可读文字          | 有成功记录；输出的是文字结果，不是完整还原的程序 |
| **被故意打乱的执行顺序**            | 尝试整理部分具有固定模式的绕路结构        | 仅支持部分形式，已有记录仍存在“部分完成”    |
| **部分干扰分析的小花招**            | 将特定的绕路跳转、干扰指令替换成更容易分析的形式 | 只处理代码中预设的几种情况            |

**目前不能自动处理的主要有：**

- VMProtect、Themida、WinLicense、Enigma Protector 等重保护。
- CodeVM 等把程序改成自定义指令运行的复杂混淆。
- 无法确定类型的未知壳。
- Linux、macOS、Android 等文件的脱壳、反混淆——目前可以识别部分文件类型，但没有接入相应处理工具。

你介绍自己的模块时，可以说：

> 目前接入了 UPX、FSG、MPRESS、ASPack 和 Enigma Virtual Box 的脱壳处理，以及部分 .NET 反混淆、隐藏字符串恢复和执行顺序整理。其中 UPX、FSG、部分 .NET 样例和字符串恢复已有成功记录，其他类型按样本条件尝试处理，并明确记录未完成的部分。



服务器上目前部署并接入了 **8 个相关工具：3 个脱壳工具、4 个反混淆或辅助处理工具、1 个识别工具。**

|分类|工具|在项目里负责什么|
|---|---|---|
|脱壳|**UPX**|去掉 UPX 壳|
|脱壳|**unipacker**|尝试处理 FSG、ASPack、MPRESS，以及部分 UPX 文件|
|脱壳|**evbunpack**|拆开 Enigma Virtual Box 打包的程序，提取其中的文件|
|反混淆|**de4dot**|尝试还原 Confuser / ConfuserEx 等保护过的 .NET 程序|
|文字恢复|**FLOSS**|提取被隐藏、加密或临时拼出来的文字|
|反混淆|**deflat**|尝试整理被故意打乱的程序执行顺序|
|辅助处理|**normalize-anti**|项目内实现，清理几种特定的绕路跳转和干扰分析的指令|
|识别|**DIE（Detect It Easy）**|判断文件用了什么壳或保护，本身不负责脱壳|

部署入口位于：

`/home/dev/BinSight/BinSight/.claude/skills/auto-reverse/`

我刚核实了这 8 个工具的文件，以及配置中要求的运行程序，均存在。本次只读检查了部署情况，没有运行样本；实际能否处理成功仍取决于样本。


项目已经给这些工具做了统一入口：**指定要处理的文件、选择工具、写明使用理由，系统就会调用工具并检查结果。**

下面是服务器上的使用示例，仅供说明，我没有执行。`/path/sample.exe` 需要换成实际文件路径。

先进入工具目录：

```
cd /home/dev/BinSight/BinSight/.claude/skills/auto-reverse
```

**1. UPX：拆掉 UPX 壳**

把判断为 UPX 加壳的程序交给它，它会尝试解压出原来的程序内容。

```
.venv/bin/python -m scripts.main run /path/sample.exe \
  --tool upx --reason '检测到 UPX 壳，尝试脱壳'
```

成功后生成一个新的脱壳文件，原文件保留。如果是被修改过的 UPX 壳，可能失败。

**2. unipacker：通过模拟解包过程脱壳**

适用于部分 32 位 Windows 程序，例如 FSG、ASPack、MPRESS，以及部分 UPX 文件。它让程序在模拟环境里完成解包，再把展开的内容保存出来。

```
.venv/bin/python -m scripts.main run /path/sample.exe \
  --tool unipacker --reason '检测到 FSG 壳，尝试模拟脱壳' \
  --timeout 45
```

成功后生成名字以 `unpacked_` 开头的文件。项目还会检查是否真正进入了恢复后的代码。

**3. evbunpack：拆开 Enigma Virtual Box 打包文件**

把被 Enigma Virtual Box 打包的程序交给它，它会尝试分离主程序和打包进去的其他文件。

```
.venv/bin/python -m scripts.main run /path/sample.exe \
  --tool evbunpack --reason '检测到 Enigma Virtual Box 打包'
```

输出包括还原的程序，以及提取出的资源文件。它不用于解除 Enigma Protector。

**4. de4dot：处理 .NET 程序的混淆**

适用于 Confuser / ConfuserEx 等工具处理过的部分 .NET 程序。它尝试识别并清理自己支持的混淆。

```
.venv/bin/python -m scripts.main run /path/sample.exe \
  --tool de4dot --reason '检测到 .NET 混淆，尝试还原'
```

成功后生成名字带 `-cleaned` 的程序文件。清理程度取决于样本使用的保护方式。

**5. FLOSS：恢复隐藏的文字**

当程序里的网址、提示语等内容被加密，或在运行过程中临时拼出来时，可以让 FLOSS 尝试恢复。

```
.venv/bin/python -m scripts.main run /path/sample.exe \
  --tool floss --reason '怀疑存在隐藏字符串，尝试恢复'
```

输出是 `floss_strings.json`，里面记录恢复出的文字。**它不会生成一个完整去混淆的新程序。**

**6. deflat：整理被绕乱的执行顺序**

适用于部分“每执行一小段代码，都回到同一个中转位置，再决定下一步”的混淆。它尝试推算真实的先后关系，并修改副本中的跳转。

```
.venv/bin/python -m scripts.main run /path/sample.exe \
  --tool deflat --reason '发现执行顺序被打乱，尝试恢复'
```

成功后生成名字带 `_recovered` 的文件。它只支持部分固定形式，可能只恢复其中一些函数。

**7. normalize-anti：清理几种特定的干扰写法**

适用于代码里出现项目已经支持的绕路跳转、干扰指令等情况。它会在副本中把这些写法替换为更容易分析的形式。

```
.venv/bin/python -m scripts.main run /path/sample.exe \
  --tool normalize-anti --reason '发现特定干扰指令，尝试清理'
```

有实际修改时，生成名字带 `_normalized` 的文件，并保存修改记录。没有匹配到对应写法，就不会产生有效的清理结果。

**8. DIE：先识别用了什么保护**

DIE 负责检查，不负责脱壳。项目通常通过综合检测入口调用它，同时结合其他检查方法：

```
.venv/bin/python -m scripts.main detect /path/sample.exe
```

如果只想单独查看 DIE 的结果，其底层调用形式是：

```
tools/die/usr/bin/diec -j /path/sample.exe
```

输出是检测结果，例如疑似壳名称和保护信息。

通过统一入口执行前七种工具时，系统会自动建立工作副本、保存结果并检查产物，报告位置会在返回结果中给出，通常位于 `runtime/agent-*/report.json`。**这八个工具按文件情况选用，不需要每个都跑一遍。**