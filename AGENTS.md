# Obsidian Vault Rules

## 同步维护

- 修改 `AGENTS.md` 或 `CLAUDE.md` 中的任意一个文件时，必须同步修改另一个文件，并始终保持二者内容完全一致。

## 任务分类

所有请求先按是否实际修改 Markdown 文件分为两类：

1. **Markdown 修改类**：会创建、转换、整理、移动、重命名或修改 Markdown 文件。
2. **知识回答类**：只在对话中进行解释、解题、推导、辨析或总结，不写入任何 Markdown 文件。

- 分类依据是是否实际改动文件，不是回答中是否使用 Markdown 排版。
- linked note、编辑器选区、编辑器光标或附件只提供上下文，不自动构成修改授权。
- 同一请求同时包含知识回答和文件修改时，回答部分优先遵循对应科目根目录中的问答要求文件；不存在时根据相关文件内容和模型自身知识回答。文件写入部分遵循 `要求/通用要求.md` 及已触发的专项要求。
- 用户最新的明确要求高于各要求文件中的默认规则。

## 一、Markdown 修改类

### 1. 通用规则

- 在创建、转换、整理或修改任何 Markdown 笔记前，必须完整阅读并遵循 `要求/通用要求.md`。
- 必须遵循其中的 frontmatter、标题、公式、表格、图片转写、内容保留及发布前检查规范。
- 标题层级统一遵循“同一文件允许多个 H1、标题最多使用到 H3”的原则；禁止使用 H4、H5、H6 或更深标题，也不得标题跳级。
- 必须审查数学公式定界符：修复 `$ 公式 $`、`$$ 公式 $$` 等定界符内侧空格；OCR 将 `$` 误识别为成对 `&` 时，须对照来源确认后改为正确的 `$公式$` 或 `$$公式$$`。不得误改 `aligned`、`array`、`matrix`、`cases` 等 LaTeX 环境中用于对齐的 `&`。

### 2. 仓库目录结构

- 创建、移动、重命名或整理仓库中的文件与目录前，必须完整阅读并遵循 `要求/仓库目录结构指南要求.md`。
- 课程内容按科目文件夹划分；直接位于仓库根目录的科目文件夹表示当前时期，已结束时期的科目整体归档到“大一上”“大二下”等时期文件夹。
- 课程文件必须放入对应科目目录，不得以单个文件形式散放在仓库根目录或时期目录。
- 每个科目目录中的 `原件` 文件夹是该科目原始资料的统一存放位置。
- 全仓库 Markdown 修改要求统一放入 `要求` 文件夹；科目专属问答要求文件直接放在对应科目根目录；`AGENTS.md` 与 `CLAUDE.md` 作为仓库入口规则文件保留在仓库根目录。

### 3. OCR 优化

- 优化 PaddleOCR、MinerU 等工具生成的 Markdown，或重建 OCR 文件标题、目录和正文结构时，必须完整阅读并遵循 `要求/优化ocr要求.md`。
- OCR 专项要求启用后，冲突部分以专项要求为准，无冲突部分继续遵循 `要求/通用要求.md`。

### 4. 总复习文件

- 当待创建、润色、整理、完善或修改的 Markdown 文件名（不含路径和 `.md` 扩展名）为 `总复习` 时，必须完整阅读并遵循 `要求/总复习编写要求.md`。
- 文件名仅含“复习”但不完全等于 `总复习` 时，不自动套用；用户明确要求套用时除外。
- `总复习.md` 中的知识点范围以用户已经列出的内容为准；章节考题按整个章节范围收录。

### 5. 真题合订文件

- 当待创建、润色、整理、完善或修改的 Markdown 文件名（不含路径和 `.md` 扩展名）为 `真题合订` 时，必须完整阅读并遵循 `要求/真题合订编写要求.md`。
- 文件名仅含“真题”或“合订”但不完全等于 `真题合订` 时，不自动套用；用户明确要求套用时除外。
- `真题合订.md` 汇总课程 `真题整理` 目录中的全部题目，按章节与题型排列。

## 二、不修改 Markdown 的知识回答类

- 回答涉及某一科目时，必须先确定该科目的根目录，并检查直接位于科目根目录中的问答要求文件；推荐文件名为 `要求.md` 或 `科目名要求.md`。
- 科目根目录存在对应要求文件时，必须先完整读取并按其要求回答，再结合用户指定文件、linked note、选区及其他相关内容。
- 科目根目录不存在对应要求文件时，根据用户指定文件、linked note、选区及其他相关文件内容，结合模型自身知识回答。