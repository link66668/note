# Obsidian Vault Rules

## 同步维护

- 修改 `AGENTS.md` 或 `CLAUDE.md` 中的任意一个文件时，必须同步修改另一个文件，并始终保持二者内容完全一致。

## 笔记编写

- 在创建、转换、整理或修改任何 Markdown 笔记前，必须先完整阅读 `笔记编写要求.md`。
- 必须遵循其中的 frontmatter、标题、公式、表格、图片转写、内容保留及发布前检查规范。
- 除非用户明确要求采用其他格式，否则该规范是本 vault 的默认要求。

## 复习计划

- 仅当用户明确要求安排、制定、整理或修改复习计划时，才必须完整阅读并遵循 `复习计划安排要求.md`。
- 不得因普通知识问答、笔记整理、考试信息或其他非排期请求而主动创建、修改或提醒复习计划。
- 用户最新的明确要求高于该文件中的默认规则。

## 总复习文件

- 当待创建、润色、整理、完善或修改的 Markdown 文件名（不含路径和 `.md` 扩展名）为 `总复习` 时，必须在操作前完整阅读并遵循 `总复习编写要求.md`。
- **文件名 `总复习` 是本规则的自动触发条件。**文件名仅含“复习”二字但不完全等于 `总复习` 时，不自动套用该规范；用户明确要求套用时除外。
- `总复习.md` 中的知识点范围以用户已经列出的内容为准；章节考题则按整个章节范围收录，具体以 `总复习编写要求.md` 为准。
- 用户最新的明确要求高于该文件中的默认规则。

## 真题合订文件

- 当待创建、润色、整理、完善或修改的 Markdown 文件名（不含路径和 `.md` 扩展名）为 `真题合订` 时，必须在操作前完整阅读并遵循 `真题合订编写要求.md`。
- **文件名 `真题合订` 是本规则的自动触发条件。**文件名仅含“真题”或“合订”但不完全等于 `真题合订` 时，不自动套用该规范；用户明确要求套用时除外。
- `真题合订.md` 汇总课程 `真题整理` 目录中的全部题目，按章节与题型排列，具体以 `真题合订编写要求.md` 为准。
- 用户最新的明确要求高于该文件中的默认规则。
